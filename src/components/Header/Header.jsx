import NavBar from "../NavBar/NavBar";

const Header = () => {
    return (
        <header>
            <img src="" alt="logo Kasa" />
            <NavBar/>
        </header>
    );
};

export default Header;